version: 2.1
orbs:
  node: circleci/node@4.7.0
jobs:
  build: 
    executor:
      name: node/default
      tag: '10.4'
    steps:
      - checkout
      - node/width-cache:
        stepts:
          -run: npm install
      - run: npm run test
